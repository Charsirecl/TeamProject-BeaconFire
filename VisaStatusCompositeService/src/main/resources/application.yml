server:
  port: 8180

spring:
  application:
    name: VisaStatusCompositeService

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: false